"""Figure generation, comparison, and dispatch scripts.

Importable as ``scripts.<name>`` once the package is installed via
``pip install -e .``. Console entry points wired in pyproject.toml
under ``[project.scripts]``.
"""
