# Phase-2 main_benchmark — PGD-L∞ leg.
# Driven by scripts/run_main_benchmark.sh. Models/tasks mirror experiment.yaml.

experiment:
  name: main_benchmark_pgd
  phase: "2"
  seeds: [0, 1, 2, 3, 4]
  output_dir: runs/main/pgd

  models:
    - qwen2_5_vl_7b
    - llava_v1_6_mistral_7b

  tasks:
    - prostate_mri_workup
    - rad_vqa_action
    - prostate_mri_targeted

  attacks:
    - pgd_linf

  epsilons_linf: [0.0078, 0.0157, 0.0314, 0.0627, 0.1254]
